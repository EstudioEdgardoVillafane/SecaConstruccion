import { BreakSpacePipe } from './break-space.pipe';

describe('BreakSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new BreakSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
