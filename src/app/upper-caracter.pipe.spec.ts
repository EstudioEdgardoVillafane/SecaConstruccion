import { UpperCaracterPipe } from './upper-caracter.pipe';

describe('UpperCaracterPipe', () => {
  it('create an instance', () => {
    const pipe = new UpperCaracterPipe();
    expect(pipe).toBeTruthy();
  });
});
